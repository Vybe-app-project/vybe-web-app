// Re-export shim. The canonical component library lives in ../components/ui.
// Pages were generated against both paths; this keeps ONE implementation
// rather than two that silently drift apart.
export * from '../components/ui';
